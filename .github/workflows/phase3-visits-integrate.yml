name: Phase 3 Visits Verification

on:
  push:
    branches:
      - futureweb-production-backend
    paths:
      - src/App.tsx
      - src/lib/visits.ts
      - supabase/migrations/202608210004_visits_clinical_records_authority.sql
      - .github/workflows/phase3-visits-integrate.yml

permissions:
  contents: read

concurrency:
  group: phase3-visits-futureweb-production-backend
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: futureweb-production-backend

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm ci --no-audit --no-fund

      - name: Type-check
        run: npm run typecheck

      - name: Build
        env:
          VITE_SUPABASE_URL: https://nbsvrzypypmmuvlgdpln.supabase.co
          VITE_SUPABASE_PUBLISHABLE_KEY: phase3-build-placeholder
        run: npm run build
